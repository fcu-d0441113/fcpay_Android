<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.fcpay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.HomeViewController">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login.OAuthLoginViewController"
            android:label="OAuth登入" />
        <activity
            android:name=".Order.OrderTable.OrderTableViewController"
            android:label="今日餐點" />
        <activity android:name=".Order.OrderTable.OrderTableViewModel" />
        <activity android:name=".Order.OrderDetail.OrderDetailUIViewModel" />
        <activity
            android:name=".Order.OrderDetail.OrderDetailUIViewController"
            android:label="餐點確認" />
        />
        <activity
            android:name=".Order.PlentyOrder.OrderPlentyViewController"
            android:label="訂購餐點" />
        <activity android:name=".Payment.SunnyBankPaymentVM" />
        <activity android:name=".Payment.SunnyBankPaymentVC"
            android:label="iSunny付款" />/>
        <activity android:name=".Order.OrderRecord.OrderRecordViewModel" />
        <activity android:name=".Order.OrderRecord.OrderRecordViewController"
            android:label="訂單紀錄" />
    </application>

</manifest>